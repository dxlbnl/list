# Sprint — current

> The one working-state file: a sprint goal + active tasks + a **Next** pointer + an append-only run
> log. Resume from **Next**. At sprint close: retro → learnings become atoms → **archive** the sprint
> to `wiki/sprint-archive/NNNN-slug.md`, then clear this file for the next sprint.

## Sprint goal

<!-- one line: the outcome this sprint is driving toward -->

_No active sprint._ The sync overhaul shipped — see [sprint-archive/0001-sync-overhaul.md](sprint-archive/0001-sync-overhaul.md).

## Tasks

- [ ] (pull cards from `wiki/backlog/` here by slug and get a go-ahead to start)

**Next:** Compose the next sprint from `wiki/backlog/` (persistent-groups, gift-lists, group quirks, …).
One outstanding op from the last sprint: apply `drizzle/0001` to Neon (`pnpm db:migrate` / `db:push`).

## Run log

<!-- append-only: one-liners at meaningful steps; archived verbatim at close -->
